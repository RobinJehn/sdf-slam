[project]
name = "sdf-slam-viz"
version = "0.1.0"
description = "Plotting scripts for sdf-slam run outputs"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "scipy>=1.17.1",
]

[dependency-groups]
dev = ["pytest>=8"]
